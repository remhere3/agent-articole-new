# Dependinte doar pentru dezvoltare / teste (nu sunt necesare in productie).
# Instalare:  pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0.0
pytest-asyncio>=0.23.0
respx>=0.21.0       # mock HTTP pentru testele de provideri (nivel 2)
